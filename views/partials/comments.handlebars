{{!-- <div class="comments">
    {{#each this}}
        <div>
            <p class="card-body2">
                {{comment.commentContent}}
                -
                {{user.username}}
                {{format_date dateCreated}}
            </p>
        </div>
    {{/each}}
</div> --}}

<div class="comments">
    {{#each this}}
        <section class="comment">
            <div class="meta">
                {{user.username}}
                on
                {{format_date dateCreated}}
            </div>
            <div class="text">
                {{commentContent}}
            </div>
        </section>
    {{/each}}
</div>